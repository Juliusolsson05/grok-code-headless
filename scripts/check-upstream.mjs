// Upstream drift check — placeholder until Task 9 wires the real npm registry
// watch (grok ships via install script, not npm; the watch target and its
// acceptance flow are decided there). Exits 0 so `check` stays honest today.
console.log('upstream:check — not yet wired (see agent-code#832 Task 9)')
