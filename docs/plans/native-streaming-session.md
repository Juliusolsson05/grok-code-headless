# Native Streaming Session

Continues the approved Grok provider plan, inline implementation and
read-only orchestration reviews. No merge/release authorization.

1. Verify cache origin fencing with the installed CLI, two real loopback
   catalogs, replayed response fixtures and an isolated HOME/GROK_HOME.
   Use a fixture-only API key, never copy real auth or modify real caches.
   Verify the second native startup fetches the new catalog rather than
   reusing the first relay's cached inference endpoint.
2. Add owned relay startup before PTY spawn, forwarded stream observations,
   and shutdown/failed-spawn cleanup. Catalog and inference must have the
   same per-relay origin. Native credential refresh remains provider-owned.
   Keep per-request streams unclassified; don't declare every sidecar a
   main-agent turn simply because it came through this session's relay.
3. Capture a real permission card using a recorded tool call against a
   disposable test cwd, then pin the screen signature and option-to-key
   mapping before implementing any answer API. A pending tool is not proof
   of an approval request, and stale screen actions must be refused.
4. Run deterministic tests and the explicit native CLI fixture-backed gate;
   request orchestrated review and address findings before the app adapter.

The full Agent Code provider still needs transcript rewrite/caught-up
semantics, parser conversion, UI rows, orchestration membership and packaging.
